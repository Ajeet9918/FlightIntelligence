# Server port (equivalent of Flask's default port 5000; Spring Boot defaults to 8080)
server.port=8080

# App name
spring.application.name=airline-booking-app

# Thymeleaf — disable caching during development (equivalent of Flask debug=True)
spring.thymeleaf.cache=false
